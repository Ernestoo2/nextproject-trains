import { TRAIN_CLASSES, TRIP_TYPES } from "@/types/shared/trains";

export const DEFAULT_STATIONS = {
  DEPARTURE: "Elelewon Railway Station",
  ARRIVAL: "Umuahia Junction",
};

export { TRAIN_CLASSES, TRIP_TYPES };
